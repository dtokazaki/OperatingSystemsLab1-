type struct Process {
    char name;
    int arrivalTime;
    int expectedTotalRuntime;
    int priority;
    int startTime;
    int completionTime;
    int runtimeRemaining;
};

Process[] queue;

char* HPF(Process[] &queue) {
    // First, sort array by priority

    // Then, sort each priority level by fcfs

    // Calculate times

    // create and return concatentation of names
}
